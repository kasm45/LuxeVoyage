@model int
@if (Model > 0)
{
    <span class="inline-flex min-h-5 shrink-0 items-center justify-center rounded-full bg-orange-600 px-1.5 text-[11px] font-semibold tabular-nums leading-none text-white min-w-[1.25rem]">@Model</span>
}
