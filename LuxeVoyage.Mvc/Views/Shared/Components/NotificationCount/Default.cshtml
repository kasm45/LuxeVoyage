@model int
@if (Model > 0)
{
    <span class="inline-flex min-h-[1.125rem] min-w-[1.125rem] shrink-0 items-center justify-center rounded-full bg-orange-600 px-1 py-0.5 text-[10px] font-semibold tabular-nums leading-none text-white sm:text-[11px]">@Model</span>
}
